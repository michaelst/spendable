name: Tests
on: push
env:
  MIX_ENV: test
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  OTP_VERSION: 24.3
  ELIXIR_VERSION: 1.13.3

jobs:
  backend:
    name: Backend
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: backend

    env: 
      TEST_DB_PASSWORD: postgres
      MIX_ENV: test

    services:
      db:
        image: postgres:11
        ports: ['5432:5432']
        env:
          POSTGRES_PASSWORD: postgres

    steps:
      - uses: actions/checkout@master

      - uses: erlef/setup-beam@v1.7.2
        with:
          otp-version: ${{ env.OTP_VERSION }}
          elixir-version: ${{ env.ELIXIR_VERSION }}

      - name: Cache Backend Deps
        uses: actions/cache@master
        with:
          path: backend/deps
          key: ${{ runner.os }}-mix-cachev2-${{ hashFiles(format('{0}{1}', github.workspace, '/backend/mix.lock')) }}
          restore-keys: |
            ${{ runner.os }}-mix-cachev2-

      - name: Cache Backend Build
        uses: actions/cache@master
        with:
          path: backend/_build
          key: ${{ runner.os }}-build-cachev2-${{ hashFiles(format('{0}{1}', github.workspace, '/backend/mix.lock')) }}
          restore-keys: |
            ${{ runner.os }}-build-cachev2-

      - run: mix do deps.get, compile --warnings-as-errors, ecto.reset
      - run: mix coveralls.json

      - uses: codecov/codecov-action@master

  credo:
    name: Credo
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: backend

    env: 
      MIX_ENV: test

    steps:
      - uses: actions/checkout@master

      - uses: erlef/setup-beam@v1.7.2
        with:
          otp-version: ${{ env.OTP_VERSION }}
          elixir-version: ${{ env.ELIXIR_VERSION }}

      - name: Cache Backend Deps
        uses: actions/cache@master
        with:
          path: backend/deps
          key: ${{ runner.os }}-mix-cachev2-${{ hashFiles(format('{0}{1}', github.workspace, '/backend/mix.lock')) }}
          restore-keys: |
            ${{ runner.os }}-mix-cachev2-

      - name: Cache Backend Build
        uses: actions/cache@master
        with:
          path: backend/_build
          key: ${{ runner.os }}-build-cachev2-${{ hashFiles(format('{0}{1}', github.workspace, '/backend/mix.lock')) }}
          restore-keys: |
            ${{ runner.os }}-build-cachev2-

      - run: mix do deps.get, compile --warnings-as-errors
      - run: mix credo

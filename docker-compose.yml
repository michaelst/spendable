services:
  db:
    image: postgres:18-alpine
    environment:
      POSTGRES_PASSWORD:
    volumes:
      - postgres-data:/var/lib/postgresql
    ports:
      - "5432:5432"
    logging:
      driver: none

volumes:
  postgres-data:
